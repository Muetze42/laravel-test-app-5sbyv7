# Laravel + Nova Example App

```shell
php artisan storage:link
php artisan migrate:fresh --seed
```

Seeder: `admin@example.com` and 50 random account with `password` as password

Nova URL default: [http://localhost/](http://localhost/)
